export default class Immobile {
  constructor (tipo, endereco, finalidade, valor, imagem) {
    this.tipo = tipo
    this.endereco = endereco
    this.finalidade = finalidade
    this.valor = valor
    this.imagem = imagem
  }
}
